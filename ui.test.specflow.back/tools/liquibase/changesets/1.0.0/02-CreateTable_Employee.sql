CREATE TABLE IF NOT EXISTS Employee (
	Id BIGINT CONSTRAINT PK_Employee PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	Name VARCHAR(100) NOT NULL,
	Email VARCHAR(100) NOT NULL,
	Active BOOLEAN NOT NULL
);