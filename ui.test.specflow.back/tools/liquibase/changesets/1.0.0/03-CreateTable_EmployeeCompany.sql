CREATE TABLE IF NOT EXISTS EmployeeCompany (
	Id BIGINT CONSTRAINT PK_EmployeeCompany PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	IdEmployee BIGINT REFERENCES Employee(Id) NOT NULL,
	IdCompany BIGINT REFERENCES Company(Id) NOT NULL,
	FromDate DATE NOT NULL,
	ToDate DATE NULL
);